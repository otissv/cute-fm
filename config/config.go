package config

// This file previously contained TOML-based configuration helpers (for
// example, parsing a "cute.toml" file). All active configuration is now
// Lua-based and implemented in lua_config.go.
